# Luma covers — Meetup 13

Generated by `scripts/luma-cover/` (vgpu backdrops + Figtree type). Stills are 1080x1080 PNG, loops are 640px GIFs at 18 fps.

- **Halftone sun** (`01-halftone-sun`) — The site's own hero motif: a flat blue disc whose rim breathes, halftone dots thickening toward the edge.
- **Liquid glass** (`02-liquid-glass`) — Metaballs read as thick glass: the backdrop refracts through them, with a Fresnel rim and a hard specular.
- **Aurora** (`03-aurora`) — Three bands of light drift across a deep ink ground, like the string lights over the long table.
- **Chrome ripple** (`04-chrome-ripple`) — A ripple lit like brushed metal: rings expand from the centre with diffuse and specular shading.
- **LED board** (`05-led-board`) — A dot-matrix board on black. A wave rolls out from the centre and single pixels twinkle over it.
- **Paper cut** (`06-paper-cut`) — The flat editorial motif straight from the site: hard-edged arcs and blocks in ink, sand and blue, sparkles turning.
- **Frosted card** (`07-frosted-card`) — Fluid colour outside, the same field seen through a frosted glass card in the middle where the type sits.
- **Spotlight** (`08-spotlight`) — A stage: one cone of light from above through drifting dust, a glow where the mark sits, a glint on the floor.
- **Brand gradient** (`09-brand-gradient`) — Closest to the covers so far: the brand blue gradient, with a slow light sweep and film grain.
